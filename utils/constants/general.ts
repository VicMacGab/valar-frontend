export const MIN_USERNAME_SIZE = 4;
export const MAX_USERNAME_SIZE = 32;

export const MIN_PASSWORD_SIZE = 8;
export const MAX_PASSWORD_SIZE = 64;

export const SECURE_PASSWORD_REGEX =
  /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[a-zA-Z\d]{8,64}$/;
